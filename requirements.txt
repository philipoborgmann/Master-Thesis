# Core runtime dependencies for the thesis pipeline.
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
pyarrow>=12.0
openpyxl>=3.1
scikit-learn>=1.3
# joblib + threadpoolctl are imported directly by the panel-logit chunk
# parallelism (--n-jobs); declared explicitly (not just via scikit-learn).
# joblib>=1.4 is required for Parallel(return_as="generator_unordered").
joblib>=1.4.2
threadpoolctl>=3.1
scipy>=1.10
statsmodels>=0.14
# Unit-root tests (DFGLS / Phillips-Perron) and structural-break detection are
# imported directly by the stationarity + structural-breaks stages.
arch>=5.3
ruptures>=1.1
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65
vaderSentiment>=3.3

# For data acquisition (Crypto _data.py)
ccxt>=4.0

# For transformer-based sentiment scoring. Install only if running FinBERT /
# CryptoBERT (heavy):
#   pip install -r requirements.txt
#   pip install torch transformers accelerate
# torch>=2.0
# transformers>=4.30
# accelerate>=0.20

# Tests
pytest>=7.0
